<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <span><%=link_to "DIT Amazon", root_path, class: "navbar-brand", id: "logo"%></span>
      <span class="beta">Beta</span>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <!-- customers -->
        <% if signed_in? and @current_customer.admin? %>
            <li><%= link_to "Customers", customers_path, id: "logo"%></li>
        <% end %>
        <!-- add here -->
        <!-- end here -->
      </ul>
    <!-- end class="nav navbar-nav"  -->
    <!-- Sign up/in/out -->
      <ul class="nav navbar-nav navbar-right">
        <% if !signed_in? %>
          <li><%= link_to "Sign in", login_path , id:"logo"%></li>
          <li><%= link_to "Register", new_customer_path, id:"logo"%></li>
        <% else %>
          <li><%= link_to "Sign out", logout_path, method: :delete,id: "logo" %></li>
          <li><%= link_to "View Profile", customer_path(@current_customer), id: "logo" %></li>
        <%end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
