
<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target="bs-navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      <span><%= link_to "Amazon", root_path, class: "navbar-brand", id: "logo" %></span>
      <span class="beta">Beta</span>
    </div>

    <ul class="nav navbar-nav">
      <% if signed_in? and @current_customer.admin? %>
          <li><%= link_to "Customers", customers_path, id: "logo"%></li>
      <% end %>
    </ul>

    <ul class="nav navbar-nav navbar-right">
      <% if !signed_in? %>
        <li><%= link_to "Sign in", login_path , id:"logo"%></li>
        <li><%= link_to "Register", new_customer_path, id: "logo"%></li>
      <% else %>
        <li><%= link_to "Sign out", logout_path, method: :delete,id: "logo" %></li>
              <li class="dropdown" , id="logo">
              	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false", id= "logo"><%= @current_customer.f_name%><span class="caret"></span></a>
                  <ul class="dropdown-menu">
                  		<li><%= link_to "Edit Profile", edit_customer_path(@current_customer) %> </li>
                  		<li><%= link_to "View Profile", customer_path(@current_customer) %></li>
                  		<li><%= link_to "View cart", root_path %></li>
                      <li role="separator" class="divider"></li>
                      <li><%= mail_to "", "e-Mail us" %></li>
                  </ul>
              </li>
        	<%end%>
    </ul>
    </div>
  </div>
</nav>
